[qc]
# Minimum sequence length (bp) — shorter sequences are removed
min_length = 200

# Maximum percentage of ambiguous bases (N) allowed
# Sequences above this threshold are flagged (not removed)
max_ambiguous_pct = 50.0
